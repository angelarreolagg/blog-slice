---
title: "Prueba de pipeline"
description: "Un archivo temporal para verificar el pipeline de MDX."
date: 2026-01-01
tags: ["prueba"]
draft: false
---

Un párrafo con `código en línea` y una _cursiva_.

```ts
export function greet(name: string): string {
  // a comment
  const count = 2;
  return `hola ${name} ${count}`;
}
```
